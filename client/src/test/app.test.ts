/**
 * Example test suite — verifies the test runner is configured correctly.
 * Replace with real tests as features are implemented.
 */
import { describe, it, expect } from 'vitest'

describe('Test suite setup', () => {
  it('should run a basic test', () => {
    // Sanity check: the test environment is working
    expect(true).toBe(true)
  })
})
